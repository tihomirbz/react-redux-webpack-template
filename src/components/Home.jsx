import React from 'react';

const Home = () => {
    return (
        <div className='body'>
            <h1>Welcome</h1>
            <p>A barebones React + Redux + React-Router template</p>
            <p>Uses webpack with preconfigured:</p>
            <ul>
                <li> Babel for JSX, ES7</li>
                <li> CSS and SASS loaders</li>
                <li> Devserver </li>
            </ul>
        </div>
    );
};

export default Home;
